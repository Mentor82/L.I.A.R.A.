# Shared Scaffold

Future shared package layout for schemas, contracts, config, and utilities
once the monorepo is split into dedicated services.

Canonical shared runtime code lives in `services/shared`, `services/contracts`, and
`services/config`.
