FROM python:3.11-slim

WORKDIR /app

COPY requirements*.txt ./
RUN pip install --no-cache-dir -r requirements.txt "uvicorn[standard]"

COPY . .

ENV PYTHONPATH=/app

EXPOSE 8020

CMD ["uvicorn", "services.memory.app:app", "--host", "0.0.0.0", "--port", "8020"]
