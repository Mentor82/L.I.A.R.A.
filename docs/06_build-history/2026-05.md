# Build-History 2026-05

## 2026-05-01 - Ist-Stand-Dokumentation strukturiert

### Geaendert

- Neue mehrschichtige Doku-Struktur unter `docs/` angelegt:
  - `00_index.md`
  - `01_architektur/`
  - `02_services/`
  - `03_apis/`
  - `04_runbooks/`
  - `05_decisions/`
  - `06_build-history/`
  - `07_tests/`
  - `08_security/`
  - `09_reference/`
- Aktuellen Code-/Konfigurationsstand dokumentiert:
  - Service-Grenzen
  - API-Flaeche
  - Runtime-Ports
  - Memory-Adapter-Entscheidung
  - Security-Grenzen
  - Testlandschaft

### Nicht geaendert

- Kein Runtime-Code veraendert.
- Keine bestehenden flachen Doku-Dateien verschoben oder geloescht.
- Keine Logs, Backups oder Build-Artefakte bereinigt.

### Validierung

- Dokumentation basiert auf lokaler Code- und Konfigurationssicht.
- Es wurde kein Live-Service-Smoke ausgefuehrt.
- Der Projektroot ist aktuell kein Git-Repository; keine Commit-SHA verfuegbar.

## 2026-05-01 - Embedding-Zieldefinition ergaenzt

### Geaendert

- ADR fuer nativen C++ OpenVINO Embedding Service als Primary ergaenzt:
  - `docs/05_decisions/adr-002-cpp-openvino-embedding-primary.md`
- `docs/02_services/liara-embedding.md` um Zielbild erweitert:
  - `LiaraEmbeddingService.exe` als primaerer Hot Path
  - Python-Embedding nur noch Fallback/Degraded Path
  - Dims/Device/Shapes konfiguriert statt hart verdrahtet

### Nicht geaendert

- Kein Runtime-Code implementiert.
- Kein bestehender Python-Embedding-Service entfernt.

## 2026-05-01 - C++ EmbeddingServer initial umgesetzt

### Geaendert

- Initialer nativer Embedding-Service im NPU-Client angelegt:
  - `C:\ai\LIARA-NPU-CLIENT\src\openviono\embedding-server`
- CMake-Target `LiaraEmbeddingService` im NPU-Client ergaenzt.
- `build.ps1` im NPU-Client haertet MSVC/VC++-Compiler-Cache-Erkennung.
- ADR-002 Status von `geplant` auf `in Umsetzung` gesetzt.

### Validierung

- VC++ Direktbuild erfolgreich:
  - `C:\ai\LIARA-NPU-CLIENT\build-msvc-manual\LiaraEmbeddingService.exe`
- `--help` erfolgreich ausgefuehrt.
- Readiness-Smoke mit ungueltigem Modellpfad liefert kontrolliert `503 Service Unavailable`.

### Einschraenkung

- Kein Live-Test mit echtem Qwen3-Embedding-OpenVINO-Modell wurde ausgefuehrt.
- Python-Fallback-Umschaltung im LIARA-Hauptsystem ist noch nicht verdrahtet.

## 2026-05-01 - Embedding/LineP Integrationsgrenze dokumentiert

### Geaendert

- Lokale Embeddingserver-Doku um die Grenze HTTP/API vs. LiNeP erweitert:
  - `src/emeddingserver/README.md`
  - `src/emeddingserver/docs/integration-boundary.md`
- ADR-002 ergaenzt:
  - Orchestrator bleibt API-basiert.
  - LiNeP ist fuer Scheduler/Helper und Scheduler/Embedding vorgesehen.

### Bedeutung

- Der EmbeddingService soll beide Oberflaechen bedienen.
- LiNeP ersetzt nicht die Orchestrator-API.

## 2026-05-01 - EmbeddingServer mit LiNeP verbunden

### Geaendert

- `src/emeddingserver` erweitert:
  - `embedding_linep.hpp`
  - `embedding_linep.cpp`
- `EmbeddingConfig` um `[linep]`-Parameter erweitert.
- `LiaraEmbeddingService` startet bei `linep.enabled=true`:
  - UDP Heartbeat als Embedding-Slot
  - TCP Handler fuer `EMBED_REQUEST -> EMBED_RESPONSE`
- Standalone-CMake bindet lokale `linep`-Library ein.

### Validierung

- VC++ Direktbuild des lokalen Snapshots erfolgreich.
- `LiaraEmbeddingService.exe --help` erfolgreich mit OpenVINO Runtime-DLLs im `PATH`.

### Noch offen

- LiNeP Similarity/Consensus Requests.
- Live-Test gegen echtes Embedding-Modell.
- API-/Memory-Fallback-Umschaltung auf C++ Primary.
